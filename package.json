{
  "name": "e2mail",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace root: shared TS package + Expo mobile client. The Go backend and Vite web client keep their own tooling.",
  "workspaces": [
    "shared",
    "mobile"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspace @e2mail/mobile"
  }
}
